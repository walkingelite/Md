"""Domain vertical packs — the vocabulary a specific industry puts on the
generic object graph.

A pack supplies services, resource types, document types and coding systems.
It never introduces new entity shapes: the moment a vertical needs a structural
change rather than a vocabulary change, the generic model is wrong and should
be fixed rather than special-cased.
"""
